---
import BaseLayout from "../layouts/BaseLayout.astro";

const LEVELS = [
  {
    badge: "（不显示徽章）",
    level: "human",
    title: "Human",
    desc: "纯人写。2016–2020 迁移自旧 Hexo 博客的 58 篇文章全部属于此类，内容保持原貌，不做 AI 重写。",
  },
  {
    badge: "✦ AI co-authored",
    level: "co-authored",
    title: "Co-authored",
    desc: "人写主体，AI 协助润色表达、补充资料或核对代码示例。观点与结论由人负责。",
  },
  {
    badge: "🤖 Agent written",
    level: "agent",
    title: "Agent written",
    desc: "Claude Code Agent 按公开规范起草，人工审核事实与语气后发布。作者署名保留 AI 参与记录。",
  },
];
---

<BaseLayout title="AI" description="本博客的 AI 使用宣言：人机分工、内容披露等级与数据开放策略。">
  <div class="py-10 sm:py-14">
    <p class="font-mono text-xs tracking-widest text-faint uppercase">AI Manifesto</p>
    <h1 class="mt-3 text-3xl font-bold tracking-tight sm:text-4xl">
      AI 在这里的<span class="text-gradient">四层存在</span>
    </h1>
    <p class="mt-4 max-w-2xl leading-relaxed text-muted">
      AI 不是这个博客的贴纸，而是基础设施。从内容生产到站点协议，每一层的参与方式与边界都公开如下 —— 这是本博客的 AI 使用宣言。
    </p>

    <div class="mt-12 grid gap-5 sm:grid-cols-2">
      <section class="rounded-2xl border border-line bg-surface p-6">
        <p class="font-mono text-xs text-accent">L1 · 内容层</p>
        <h2 class="mt-2 text-lg font-semibold">披露等级</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          每篇文章的 frontmatter 都有 <code class="rounded bg-elevated px-1.5 py-0.5 font-mono text-xs">ai</code> 字段，
          全站渲染为徽章，读者一眼可辨内容来源。宁可多披露，不可少披露。
        </p>
        <ul class="mt-4 space-y-3 text-sm">
          {
            LEVELS.map((l) => (
              <li class="rounded-xl border border-line/60 bg-elevated/50 p-3">
                <p class="font-medium">
                  {l.title} <span class="ml-1 text-xs text-faint">{l.badge}</span>
                </p>
                <p class="mt-1 leading-relaxed text-muted">{l.desc}</p>
              </li>
            ))
          }
        </ul>
      </section>

      <section class="rounded-2xl border border-line bg-surface p-6">
        <p class="font-mono text-xs text-accent">L2 · 页面层</p>
        <h2 class="mt-2 text-lg font-semibold">可见的协作</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          首页终端卡片展示的是真实工作流：<span class="text-default">人出题，Agent 起草，npm run build 闸门校验，人工终审后合并发布</span>。
          完整写作规范开源在仓库 <code class="rounded bg-elevated px-1.5 py-0.5 font-mono text-xs">AGENTS.md</code> 与
          <code class="rounded bg-elevated px-1.5 py-0.5 font-mono text-xs">docs/AI-EDITORIAL.md</code>。
        </p>
        <h3 class="mt-5 text-sm font-semibold">底线</h3>
        <ul class="mt-2 list-disc space-y-1.5 pl-5 text-sm leading-relaxed text-muted">
          <li>Agent 不得编造事实、链接、版本号与基准数据</li>
          <li>代码示例必须实际跑通过</li>
          <li>任何 AI 参与超过润色级别的内容必须标记 <code class="font-mono text-xs">agent</code></li>
        </ul>
      </section>

      <section class="rounded-2xl border border-line bg-surface p-6">
        <p class="font-mono text-xs text-accent">L3 · 协议层</p>
        <h2 class="mt-2 text-lg font-semibold">对 Agent 友好</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          站点对 AI 爬虫与 Agent 开放：
          <a class="text-accent hover:underline" href="/llms.txt">/llms.txt</a> 提供站点导览，
          <a class="text-accent hover:underline" href="/robots.txt">robots.txt</a> 显式欢迎主流 AI 爬虫，
          <a class="text-accent hover:underline" href="/rss.xml">RSS</a> 输出全站更新。引用请附原文链接。
        </p>
      </section>

      <section class="rounded-2xl border border-line bg-surface p-6">
        <p class="font-mono text-xs text-accent">L4 · 流程层</p>
        <h2 class="mt-2 text-lg font-semibold">发布流水线</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          选题（人）→ 起草（Claude Code）→ 构建闸门（schema 校验 + build）→ 终审（人）→ CI 自动部署。
          2026 年起新文章逐步由该流水线生产，历史文章保持纯人写。
        </p>
        <div class="mt-4 overflow-x-auto rounded-xl border border-line bg-elevated/50 p-4 font-mono text-xs leading-relaxed">
          <p class="text-faint">$ npm run new-post -- "标题" --tags ai,tooling</p>
          <p class="text-faint">$ npm run build  <span class="text-emerald-400"># schema + 渲染自检</span></p>
          <p class="text-faint">$ git commit -m "post: …" && 开 PR</p>
        </div>
      </section>
    </div>

    <p class="mt-10 text-sm text-faint">
      对某篇内容的人机比例有疑问？<a class="text-accent hover:underline" href="https://github.com/itabas016">GitHub</a> 上开 Issue，我来回答。
    </p>
  </div>
</BaseLayout>
